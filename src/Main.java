import javafx.application.Application;

// the true entry point of the app
public class Main {
    public static void main(String[] args) {
        Application.launch(ui.ChatApplication   .class, args);
    }
}